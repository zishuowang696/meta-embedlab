# TinyAI 发行版配置
# 继承自 poky，默认包含 llama-cpp（LLM 推理引擎）

require conf/distro/poky.conf

DISTRO_NAME = "TinyAI"
DISTRO_VERSION = "1.0.0"
DISTRO_CODENAME = "tinyai"

# 默认集成 llama-cpp 到所有镜像
DISTRO_EXTRA_RDEPENDS += "llama-cpp"

# 使用 OpenRC 作为 init 系统（轻量、并行启动）
INIT_MANAGER = "openrc"
