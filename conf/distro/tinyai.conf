# TinyAI 发行版配置
# 继承自 poky，默认包含 llama-cpp（LLM 推理引擎）

require conf/distro/poky.conf

DISTRO_NAME = "TinyAI"
DISTRO_VERSION = "1.0.0"
DISTRO_CODENAME = "tinyai"

# 使用 OpenRC（轻量级 init 系统，适合嵌入式 + musl）
INIT_MANAGER = "openrc"

# 使用 musl libc（减少体积，适合嵌入式）
TCLIBC = "musl"
